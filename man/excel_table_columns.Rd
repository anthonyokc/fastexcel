% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_excel.R
\name{excel_table_columns}
\alias{excel_table_columns}
\title{Inspect Excel table column metadata in a workbook}
\usage{
excel_table_columns(
  file,
  table,
  columns = NULL,
  col_names = TRUE,
  header_row = NULL,
  skip_rows = NULL,
  n_max = Inf,
  schema_sample_rows = NULL,
  dtype_coercion = c("coerce", "strict"),
  dtypes = NULL,
  skip_whitespace_tail_rows = FALSE,
  whitespace_as_null = FALSE,
  available = FALSE
)
}
\arguments{
\item{file}{Path to an Excel workbook, or a raw vector containing workbook
bytes.}

\item{table}{Name of the Excel table to read.}

\item{columns}{Optional table columns to select, as a character vector of
column names or a numeric vector of 1-based column positions.}

\item{col_names}{\code{TRUE} to use the first row as names, \code{FALSE} to generate
names, or a character vector of explicit names.}

\item{header_row}{1-based row containing column names when \code{col_names = TRUE}.
Use \code{NULL} to let the reader use its default first non-empty row behavior.}

\item{skip_rows}{Number of rows to skip after the header row. Use \code{NULL} for
the upstream default of skipping only empty leading rows.}

\item{n_max}{Maximum number of data rows to read.}

\item{schema_sample_rows}{Number of rows to sample for schema inference. Use
\code{NULL} to sample all rows.}

\item{dtype_coercion}{How to handle cells that do not match the inferred or
specified type: \code{"coerce"} or \code{"strict"}.}

\item{dtypes}{Optional type override. Supply a single dtype string to apply to
all selected columns, or a named character vector mapping column names to
dtypes. Supported values are \code{"null"}, \code{"int"}, \code{"float"}, \code{"string"},
\code{"boolean"}, \code{"datetime"}, \code{"date"}, and \code{"duration"}.}

\item{skip_whitespace_tail_rows}{Whether to ignore trailing rows containing
only whitespace and null values.}

\item{whitespace_as_null}{Whether string cells containing only whitespace are
treated as missing values.}

\item{available}{If \code{FALSE}, return metadata for the selected columns. If
\code{TRUE}, return metadata for all available columns after applying header and
dtype options, ignoring \code{columns}.}
}
\value{
A tibble with one row per column and columns \code{name}, \code{index},
\code{absolute_index}, \code{dtype}, \code{column_name_from}, and \code{dtype_from}. Indices are
1-based for R.
}
\description{
Inspect Excel table column metadata in a workbook
}
